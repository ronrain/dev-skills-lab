<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Express Dev Skills</title>
</head>
<body>
  <h1>Skills</h1>
  <ul>
    <% skills.forEach(skill => { %>
      <li>
        <%= skill.text %> 
        - 
        <%= skill.active ? 'active' : ' not active' %> 
        - 
        <%= skill.play ? 'I am continuing with this skill' : 
        'I am not continuing with this skill'  %>
      </li>
    <% }) %>
  </ul>
</body>
</html>